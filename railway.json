{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "deployment/Dockerfile.backend",
        "context": "."
    },
    "deploy": {
        "startCommand": "sh -c 'uvicorn backend.server:app --host 0.0.0.0 --port ${PORT:-8000}'",
        "restartPolicyType": "ON_FAILURE"
    }
}